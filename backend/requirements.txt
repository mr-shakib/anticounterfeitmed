# Backend runtime dependencies.
#
# The local libraries medcrypto/ and signer/ are installed separately as
# editable packages; see docs/17-running-locally.md.
#
# cryptography carries the ML-DSA implementation and bundles its own OpenSSL,
# so the host's OpenSSL version does not matter. Treat a change to this pin as
# a cryptographic change: re-run crypto-vectors/ before accepting it.

Django==5.2.17
djangorestframework==3.16.1
psycopg[binary]==3.2.13
celery==5.6.3
redis==5.3.1

cryptography==50.0.1
rfc8785==0.1.4

# Staff second factor.
pyotp==2.10.0

# Backend to signing service.
requests==2.34.2

python-dotenv==1.2.3

# Production WSGI server (installed in the image; harmless locally).
gunicorn==23.0.0
