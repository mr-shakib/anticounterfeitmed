# Development and tooling dependencies, on top of backend/requirements.txt.
#
#   pip install -r backend/requirements.txt -r requirements-dev.txt

pytest==8.4.2
pytest-django==4.14.0

# The signing service's own HTTP transport. Listed here because the backend
# does not import it; only the signer image and its tests need it.
fastapi==0.141.1
uvicorn[standard]==0.53.0

# QR label generation for the physical test sheet, and brand asset resizing.
segno==1.6.6
Pillow==12.3.0
